#!/bin/bash
shopt -s expand_aliases
alias listdirs="find . -mindepth 1 -maxdepth 1 -type d -not -path '*/\.*' | sort"
cd "$( listdirs | grep "$(echo $1 | sed 's/\/$//g' )" | sed -n "$([[ -n $2 ]] && echo $2 || echo 1 )"p ;)"
ls --color=always -C --group-directories-first -w $(tput cols)
